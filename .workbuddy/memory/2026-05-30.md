
## Git push 成功 + 跨设备兼容性修复
- `.gitignore`：移除 `*.db` 排除规则，改为只排除 `*.db-journal` 和 `*.db-wal`，syaA.db 现在随仓库同步
- MySQL 迁移：密码从硬编码改为环境变量 `SYAA_MYSQL_PASSWORD`，未设置则拒绝迁移
- 播放视频：加 `sys.platform` 判断，支持 Windows/macOS/Linux
- 已成功 push 到 https://github.com/syawzzw/syaA.git (main)
