## 2026-07-13 工作日志

### 普通爬取 - 新增"爬取无码"开关 ✅已完成
- GUI新增 `Checkbutton`（"爬取无码"，默认不勾选），位于线程数输入框右侧 (L1289-1291)
- 配置持久化：`crawl_allow_uc` 字段，保存到 gui_config.json (L8642)
- 爬取循环插入无码跳过判断 (L1918-1922)：在欧美片判断之后、写磁力文件之前
  - 判断条件：`cn_type == "无码破解" and not self.crawl_allow_uc.get()`
  - 跳过时计入新统计项 `uc_skip`
- 统计初始化增加 `uc_skip: 0` (L1422)
- 统计报告输出增加"无码跳过"列 (L1518-1525)
- 修改文件：src/url/gui_app.py（4处改动，语法检查通过）