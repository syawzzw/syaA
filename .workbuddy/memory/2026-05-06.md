# 2026-05-06 工作日志

## Round 18-19: 反爬误报修复
- `/txt.html` 预检误杀正常列表页 → 改为先提取ID再判断

## Round 19: 页数异常修复  
- 正则 `(\d+)/(\d+)` 匹配到无关数字 → 限定分页上下文 + 10万上限

## Round 20: 流式全站爬取重构 ⭐
- 两阶段 → 流式单阶段（爬一页立刻下载该页的书）

## Round 21: 章节增量卡顿 + 内容重复
- **卡顿根因**: `_fetch_directory_chapters` 对2382章的书要翻40~50页目录才找到3个新章，每页间隔1~2s
  - 修复: 目录翻页加进度日志(每5页) + 章节增量总超时5分钟 + 完成/无新章节日志
  - `max_pages` 上限从20提升到50（大长篇目录多）
- **内容重复根因**: 增量模式 `write_mode="a"` 追加写入，站方调整分卷结构后同一标签的卷被重新下载追加
  - 修复: 新增**标签去重机制**：
    - `already_labels`: 从 book_index 加载已下载的分卷标签集合
    - 下载前同时检查索引号(`already_parts`)和标签(`already_labels`)
    - `_update_book_entry` 新增 `downloaded_part_labels` 参数保存标签
    - 标签匹配时跳过并日志提示"标签重复跳过"
