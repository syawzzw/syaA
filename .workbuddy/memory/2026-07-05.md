# 2026-07-05

## 离线爬取功能恢复（代码丢失后重建）
- **问题**：gui_app.py 被回退到旧版本（7003行），离线爬取功能（几千行代码）全部丢失
  - git 历史里从未提交过离线爬取代码，stash 里也没有
  - `output/离线网页/` 目录和网页文件还在（之前爬取的成果），只是代码没了
- **恢复内容**（文件从 7003→8165 行，新增约 1162 行）：
  - `__init__` 新增变量：`_offline_dl_lock`、`_crawl_verify_post_id`、`_last_success_time`、`_crawl_stop_reason`
  - UI：离线并发页数 Combobox(1~4)、离线专用帖号(offline_crawl_begin/end)、离线爬取按钮
  - `_start_offline_crawl()` (L1829) — 读离线专用输入框，启动worker线程
  - `_offline_crawl_worker()` (L1876) — 核心：Cookie验证+二次验证、_downloaded.json去重、并发处理、网页/图片/CSS保存、分区白名单(含新格式正则)、主页/分区页/搜索数据增量更新
  - `_offline_crawl_insert()` (L2354) — 同步入库（复用普通爬取的11步过滤）
  - `_offline_crawl_done()` (L2505) — 完成回调，恢复按钮
  - `_update_offline_index()` (L2638) — 增量更新分区页
  - `_generate_offline_index()` (L2864) — 全量生成主页+分区页
  - `_load_search_data()` / `_update_search_data()` — 搜索数据JSON
  - `_crawl_done()` 修改：增加恢复 btn_offline_crawl
  - `_on_close()` 配置保存：offline_concurrent/offline_crawl_begin/offline_crawl_end
- **未恢复**（6/21的评分注入功能）：`_inject_rating_widget`、`_inject_rating_to_section_page`、`_import_offline_ratings` — 属于锦上添花，后续按需补
- **验证**：语法OK、模块导入OK、所有8个关键方法存在、变量/配置/正则全部完整

## 教训
- 离线爬取代码从未提交到git，一直在工作区。某次操作（可能是git checkout/reset或编辑器回退）导致丢失
- **建议**：恢复后立即 git commit，避免再次丢失
